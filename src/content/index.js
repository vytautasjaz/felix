import contentReducer from "../app/state/content";
import * as types from "./types";
import * as selectors from "./selectors";
import * as actions from "./actions";

export default { contentReducer, types, actions, selectors };